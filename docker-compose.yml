services:
  api:
    image: mridul65888/synapscaretranscript-api:latest
    container_name: transcript_api
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  main:
    image: mridul65888/synapscaresoap-api:latest
    container_name: soap_api
    ports:
      - "8001:8001"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    image: mridul65888/synaoscareai-ui:latest
    container_name: frontend
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: http://api:8000
      NEXT_PUBLIC_SOAP_API_URL: http://main:8001
    depends_on:
      - api
      - main
    restart: unless-stopped